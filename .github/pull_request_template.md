## Änderung

<!-- Was wurde geändert und warum? -->

## Tests

- [ ] Bestehende Tests wurden an das geänderte Verhalten angepasst.
- [ ] Neue Fehler- und Regressionsfälle wurden automatisiert getestet.
- [ ] `python3 -m ruff check .` meldet keine Probleme.
- [ ] `python3 -m unittest discover -s . -p 'test_*.py' -v` läuft vollständig durch.
- [ ] Relevante manuelle Tests auf Linux, Apple-Silicon-macOS und/oder Windows wurden durchgeführt.

<!-- Falls kein automatisierter Test möglich ist: Grund und manuelle Prüfung hier dokumentieren. -->
