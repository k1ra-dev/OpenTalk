[tool.ruff]
target-version = "py310"
line-length = 100
exclude = ["build", "dist", "artifacts"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "B", "SIM", "PERF"]

[tool.ruff.lint.per-file-ignores]
"tests/test_*.py" = ["SIM117"]
